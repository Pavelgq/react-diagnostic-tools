## What does this change do?

<!-- One or two sentences. -->

## Checklist

- [ ] `npm run lint`, `npm run type-check`, `npm run test:coverage`, and `npm run build` all pass
- [ ] Added/updated tests for the change
- [ ] Added/updated the hook's README if its API or behavior changed
- [ ] Added a changeset (`npm run changeset`) if this affects the published package
