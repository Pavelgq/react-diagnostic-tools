---
"@atme-lab/react-diagnostic-tools": minor
---

Add `useMemoGuard`, an on-demand A/B benchmark for `useMemo`: measures a computation both with and without the cache and logs the two timings side by side, instead of `useMemoPerformance`'s inferred cache-hit-rate estimate.
