---
"@atme-lab/react-diagnostic-tools": minor
---

Add `useTrackedEffect`, which wraps `useEffect` and reports which dependency changed and caused it to re-run - the same idea as `useWhyRender`, applied to effects instead of renders.
